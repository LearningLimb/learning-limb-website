{
    "root": "dist",
    "clean_urls": false,
    "routes": {
        "/**": "index.html"
    },
    "proxies": {
        "/auth/": {
            "origin": "${AUTH_HOST}/auth/"
        },
        "/api/": {
            "origin": "${API_HOST}/api/"
        }
    }
}
